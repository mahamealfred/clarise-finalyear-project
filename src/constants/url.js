const baseUrl = "http://127.0.0.1:5000";
const routeKey = "BCA";

export const url = {
  businessDefinition: `${baseUrl}/${routeKey}/bisinessDefinition`,
  businessIdea: `${baseUrl}/${routeKey}/bisinessIdea`,
  authentication: `${baseUrl}/${routeKey}/authentication`,
  register: `${baseUrl}/${routeKey}/register`,
};
